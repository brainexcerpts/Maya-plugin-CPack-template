# Plugin version number.
# Note: this is the only file that should be updated when the plugin
# version number is changed.

set(project_VERSION_MAJOR 1)
set(project_VERSION_MINOR 0)
set(project_VERSION_PATCH 7)

# Package release number: increment this when only modifying code relative to
# the package (usually missing files in the installer etc.)
# Reset this to 1 if the project number changes.
set(CPACK_PACKAGE_RELEASE "1")
